<Window x:Class="FleetManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fleet Manager" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA">

    <Window.Resources>
        <!-- Style pour le Menu -->
        <Style TargetType="Menu">
            <Setter Property="Background" Value="#2C3E50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#34495E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style pour les cartes de statistiques -->
        <Style x:Key="StatCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000" BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style pour DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D8E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8F9FA"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="True"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#ECF0F1"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="BorderBrush" Value="#D1D8E0"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EBF5FB"/>
                    <Setter Property="Foreground" Value="#2C3E50"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Menu supérieur -->
        <Menu DockPanel.Dock="Top" Height="45">
            <MenuItem x:Name="AdminMenu" Header="👨‍💼 Administration" Click="OpenAdmin_Click"/>
            <MenuItem Header="🏠 Accueil" Click="MenuItem_Click"/>
            <MenuItem Header="🚗 Véhicules">
                <MenuItem Header="➕ Ajouter un véhicule" Click="Menu_AddVehicle_Click"/>
                <MenuItem Header="📋 Liste des véhicules" Click="Menu_ListVehicles_Click"/>
            </MenuItem>
            <MenuItem Header="⛽ Suivi">
                <MenuItem Header="➕ Ajouter un suivi" Click="Menu_AddSuivi_Click"/>
                <MenuItem Header="📊 Voir les suivis" Click="Menu_ViewSuivi_Click"/>
            </MenuItem>
        </Menu>

        <!-- Contenu principal -->
        <Grid>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="MainContent" Margin="30,20">

                    <!-- En-tête du tableau de bord -->
                    <Border Background="White" 
                            CornerRadius="10" 
                            Padding="25"
                            Margin="0,0,0,25">
                        <Border.Effect>
                            <DropShadowEffect Color="#000" BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="📊 Tableau de Bord" 
                                       FontSize="28" 
                                       FontWeight="Bold" 
                                       Foreground="#2C3E50"
                                       HorizontalAlignment="Left"
                                       Margin="0,0,0,5"/>
                            <TextBlock Text="Vue d'ensemble de votre flotte de véhicules" 
                                       FontSize="14" 
                                       Foreground="#7F8C8D"/>
                        </StackPanel>
                    </Border>

                    <!-- Cartes de statistiques -->
                    <Grid Margin="0,0,0,25">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Carte Kilométrage -->
                        <Border Grid.Column="0" Style="{StaticResource StatCard}">
                            <StackPanel>
                                <TextBlock Text="🛣️" 
                                           FontSize="32" 
                                           HorizontalAlignment="Left"
                                           Margin="0,0,0,10"/>
                                <TextBlock Text="Kilométrage Total" 
                                           FontSize="14" 
                                           Foreground="#7F8C8D"
                                           Margin="0,0,0,5"/>
                                <TextBlock x:Name="txtKilometrageTotal" 
                                           Text="0 km" 
                                           FontSize="28" 
                                           FontWeight="Bold" 
                                           Foreground="#2C3E50"/>
                            </StackPanel>
                        </Border>

                        <!-- Carte Coût -->
                        <Border Grid.Column="1" Style="{StaticResource StatCard}">
                            <StackPanel>
                                <TextBlock Text="💰" 
                                           FontSize="32" 
                                           HorizontalAlignment="Left"
                                           Margin="0,0,0,10"/>
                                <TextBlock Text="Coût Total Carburant" 
                                           FontSize="14" 
                                           Foreground="#7F8C8D"
                                           Margin="0,0,0,5"/>
                                <TextBlock x:Name="txtCoutTotal" 
                                           Text="0 €" 
                                           FontSize="28" 
                                           FontWeight="Bold" 
                                           Foreground="#2C3E50"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Section Suivis récents -->
                    <Border Background="White" 
                            CornerRadius="10" 
                            Padding="25">
                        <Border.Effect>
                            <DropShadowEffect Color="#000" BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                        </Border.Effect>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBlock Text="📋 " 
                                           FontSize="22" 
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="Suivis Récents" 
                                           FontSize="20" 
                                           FontWeight="SemiBold" 
                                           Foreground="#2C3E50"
                                           VerticalAlignment="Center"/>
                            </StackPanel>

                            <Border BorderBrush="#D1D8E0" 
                                    BorderThickness="1" 
                                    CornerRadius="8">
                                <DataGrid x:Name="dgRecentSuivi" 
                                         Height="300" 
                                         IsReadOnly="True"/>
                            </Border>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </DockPanel>
</Window>